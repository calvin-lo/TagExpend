<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <gradient
        android:startColor="@color/accent"
        android:centerColor="@color/defaultPrimary"
        android:endColor="@color/accent"
        />
    <corners
        android:radius="2dp"
        />
</shape>